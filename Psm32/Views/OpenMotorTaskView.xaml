<UserControl x:Class="Psm32.Views.OpenMotorTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Psm32.Views"
             xmlns:cc="clr-namespace:Psm32.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <cc:ModalControl x:Name="modal" Panel.ZIndex="10">
            <Grid Width="300" Height="300">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button
                        x:Name="btnCloseModal"
                        Margin="0 10 0 0"
                        Padding="10 5"
                        Click="OnCloseModalClick"
                        Content="Close Modal" />
                </StackPanel>
            </Grid>
        </cc:ModalControl>
    </Grid>
</UserControl>
